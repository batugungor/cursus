<?php

namespace app\Providers;

use Rareloop\Lumberjack\Config;

class RegisterCustomMetaProvider
{
    public function boot(Config $config): void
    {
//        $user_id = get_current_user_id(); // Get the current user's ID
//        $lesson_id = 123; // ID of the lesson to grant access to
//        $lessons = get_user_meta($user_id, 'lesson_access', true); // Get current lesson access
////        dd(get_user_meta($user_id, 'lesson_access', true));
//        if (!is_array($lessons)) {
//            $lessons = []; // Initialize as array if it's not already
//        }
//
//        if (!in_array($lesson_id, $lessons)) {
//            $lessons[] = $lesson_id; // Add lesson if not already present
//            update_user_meta($user_id, 'lesson_access', $lessons); // Update user meta
//        }

    }

}
